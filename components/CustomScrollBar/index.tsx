import { useEffect, useRef } from "react";

const ScrollBar = () => {
  return (
    <>
      <div className="my-scrollbar">
        <div className="my-scrollbar__handle">
          <div></div>
        </div>
      </div>
    </>
  );
};

export default ScrollBar;
